{"problem_statement":"Write solve(number) to return True exactly when non-negative integer number is a power of two. Zero is not a power of two.","reference_solution":"def solve(number):\n    return number > 0 and number & (number - 1) == 0\n","test_source":"from solution import solve\n\ndef test_powers_and_boundaries():\n    assert solve(1) is True\n    assert solve(16) is True\n    assert solve(0) is False\n    assert solve(18) is False\n"}
