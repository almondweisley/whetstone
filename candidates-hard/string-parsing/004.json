{"problem_statement":"Write solve(text) to decode a run-length string made of a single uppercase letter followed by one or more decimal digits. Return the expanded string. An empty input returns an empty string.","reference_solution":"def solve(text):\n    result = []\n    index = 0\n    while index < len(text):\n        letter = text[index]; index += 1\n        start = index\n        while index < len(text) and text[index].isdigit(): index += 1\n        result.append(letter * int(text[start:index]))\n    return ''.join(result)\n","test_source":"from solution import solve\n\ndef test_multi_digit_counts():\n    assert solve('A2B10C1') == 'AABBBBBBBBBBC'\n\ndef test_empty():\n    assert solve('') == ''\n"}
