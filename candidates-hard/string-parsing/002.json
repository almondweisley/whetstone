{"problem_statement":"Write solve(expression) to evaluate a non-empty expression containing non-negative integers, +, and *, with arbitrary whitespace. Multiplication has higher precedence. Do not use eval.","reference_solution":"def solve(expression):\n    total = 0\n    for term in expression.replace(' ', '').split('+'):\n        product = 1\n        for factor in term.split('*'): product *= int(factor)\n        total += product\n    return total\n","test_source":"from solution import solve\n\ndef test_precedence_and_spaces():\n    assert solve('2 + 3 * 4 + 1') == 15\n\ndef test_single_number():\n    assert solve('0') == 0\n"}
