{"problem_statement":"Write solve(intervals) to return the maximum number of non-overlapping half-open intervals (start, end) you can select. An interval ending at t does not overlap one starting at t. For equal end times, choose the interval with the earlier start.","reference_solution":"def solve(intervals):\n    ordered = sorted(intervals, key=lambda interval: (interval[1], interval[0]))\n    count = 0\n    finish = None\n    for start, end in ordered:\n        if finish is None or start >= finish:\n            count += 1\n            finish = end\n    return count\n","test_source":"from solution import solve\n\ndef test_touching_intervals_can_both_be_selected():\n    assert solve([(1, 3), (3, 5), (2, 4), (5, 6)]) == 3\n\ndef test_empty_input():\n    assert solve([]) == 0\n"}
