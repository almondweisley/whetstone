{"problem_statement":"Write solve(items, capacity) where items is a list of (weight, value) integer tuples. Return the maximum value obtainable with total weight at most capacity; each item can be chosen at most once. Handle capacity 0.","reference_solution":"def solve(items, capacity):\n    best = [0] * (capacity + 1)\n    for weight, value in items:\n        for remaining in range(capacity, weight - 1, -1):\n            best[remaining] = max(best[remaining], best[remaining - weight] + value)\n    return best[capacity]\n","test_source":"from solution import solve\n\ndef test_zero_one_knapsack():\n    assert solve([(2, 3), (3, 4), (4, 5)], 5) == 7\n\ndef test_zero_capacity():\n    assert solve([(1, 10)], 0) == 0\n"}
