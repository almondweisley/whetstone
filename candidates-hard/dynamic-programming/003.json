{"problem_statement":"Write solve(values) to return the largest possible sum of non-adjacent values. You may choose no values, so an all-negative list returns 0. Handle an empty list.","reference_solution":"def solve(values):\n    previous_two = 0\n    previous_one = 0\n    for value in values:\n        previous_two, previous_one = previous_one, max(previous_one, previous_two + value)\n    return previous_one\n","test_source":"from solution import solve\n\ndef test_skips_adjacent_values():\n    assert solve([3, 2, 7, 10]) == 13\n\ndef test_empty_and_negative_values():\n    assert solve([]) == 0\n    assert solve([-4, -1]) == 0\n"}
