{"problem_statement":"Write solve(coins, amount) to return the number of distinct combinations of coins that total amount. Coin order does not create a new combination, every positive denomination can be reused, and amount 0 has one empty combination.","reference_solution":"def solve(coins, amount):\n    ways = [0] * (amount + 1)\n    ways[0] = 1\n    for coin in coins:\n        for total in range(coin, amount + 1):\n            ways[total] += ways[total - coin]\n    return ways[amount]\n","test_source":"from solution import solve\n\ndef test_unordered_coin_combinations():\n    assert solve([1, 2, 5], 5) == 4\n\ndef test_zero_and_impossible():\n    assert solve([2], 0) == 1\n    assert solve([2], 3) == 0\n"}
