{"problem_statement":"Write solve(text) to return a string made from the first character of every word in text. Words are separated by whitespace.","reference_solution":"def solve(text):\n    return ''.join(word[0] for word in text.split())\n","test_source":"from solution import solve\n\ndef test_initials():\n    assert solve('central processing unit') == 'cpu'\n\ndef test_one_word():\n    assert solve('Hello') == 'H'\n"}
