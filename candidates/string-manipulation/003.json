{"problem_statement":"Write solve(sentence) to return the number of words in sentence. Words are separated by one or more whitespace characters.","reference_solution":"def solve(sentence):\n    return len(sentence.split())\n","test_source":"from solution import solve\n\ndef test_multiple_spaces():\n    assert solve('one   two three') == 3\n\ndef test_blank_text():\n    assert solve('   ') == 0\n"}
