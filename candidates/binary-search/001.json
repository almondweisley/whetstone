{"problem_statement":"Write solve(numbers, target) to return the index of target in the sorted list numbers using binary search. Return -1 when target is absent.","reference_solution":"def solve(numbers, target):\n    low = 0\n    high = len(numbers) - 1\n    while low <= high:\n        middle = (low + high) // 2\n        if numbers[middle] == target:\n            return middle\n        if numbers[middle] < target:\n            low = middle + 1\n        else:\n            high = middle - 1\n    return -1\n","test_source":"from solution import solve\n\ndef test_finds_target():\n    assert solve([1, 3, 5, 7, 9], 7) == 3\n\ndef test_absent_target():\n    assert solve([1, 3, 5], 4) == -1\n"}
