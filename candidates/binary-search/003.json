{"problem_statement":"Write solve(numbers, target) to return the insertion index for target in the sorted list numbers. If target is already present, return its index.","reference_solution":"def solve(numbers, target):\n    low, high = 0, len(numbers)\n    while low < high:\n        middle = (low + high) // 2\n        if numbers[middle] < target:\n            low = middle + 1\n        else:\n            high = middle\n    return low\n","test_source":"from solution import solve\n\ndef test_insert_between_values():\n    assert solve([1, 3, 5, 7], 4) == 2\n\ndef test_existing_value():\n    assert solve([1, 3, 5, 7], 5) == 2\n"}
