{"problem_statement":"Write solve(text) as a generator function that yields each word in text, one at a time. Words are separated by whitespace.","reference_solution":"def solve(text):\n    for word in text.split():\n        yield word\n","test_source":"from solution import solve\n\ndef test_yields_words():\n    assert list(solve('one two  three')) == ['one', 'two', 'three']\n\ndef test_blank_text():\n    assert list(solve('')) == []\n"}
